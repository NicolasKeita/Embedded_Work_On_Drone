# Campaign defaults for the host SIL Monte Carlo runner.
# --seed, --runs and --scenario override these values.
# Physics distributions are configured separately in simulation.conf.
seed = 42
runs = 50
scenario = NOMINAL-001
