'use client';
import { Area, AreaChart, CartesianGrid, Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import type { TwinSnapshot } from '@/lib/twin-data';
export function TelemetryCharts({ data }: { data: TwinSnapshot[] }) { const chartData = data.map((item) => ({ time: item.time_s, altitude: item.aircraft.altitude_m, target: item.target.altitude_m, error: Math.hypot(item.aircraft.x_m - item.target.x_m, item.aircraft.y_m - item.target.y_m) })); return <div className="charts"><div><div className="chart-title"><span>ALTITUDE</span><b>ACTUAL</b><i /><b className="target">TARGET</b></div><ResponsiveContainer width="100%" height={112}><LineChart data={chartData}><CartesianGrid stroke="#18313b" vertical={false} /><XAxis dataKey="time" hide /><YAxis domain={['dataMin - 1', 'dataMax + 1']} width={34} tick={{ fill: '#607783', fontSize: 10 }} axisLine={false} tickLine={false} /><Tooltip contentStyle={{ background: '#0c1820', border: '1px solid #203943', fontSize: 11 }} /><Line dataKey="altitude" stroke="#39d8c8" strokeWidth={2} dot={false} isAnimationActive={false} /><Line dataKey="target" stroke="#8799a3" strokeDasharray="4 4" dot={false} isAnimationActive={false} /></LineChart></ResponsiveContainer></div><div><div className="chart-title"><span>POSITION ERROR</span><b>{chartData.at(-1)?.error.toFixed(2)} m</b></div><ResponsiveContainer width="100%" height={92}><AreaChart data={chartData}><defs><linearGradient id="errorFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#e2a742" stopOpacity={.35} /><stop offset="100%" stopColor="#e2a742" stopOpacity={0} /></linearGradient></defs><CartesianGrid stroke="#18313b" vertical={false} /><XAxis dataKey="time" hide /><YAxis width={34} tick={{ fill: '#607783', fontSize: 10 }} axisLine={false} tickLine={false} /><Area dataKey="error" stroke="#e2a742" fill="url(#errorFill)" strokeWidth={1.8} dot={false} isAnimationActive={false} /></AreaChart></ResponsiveContainer></div></div>; }
